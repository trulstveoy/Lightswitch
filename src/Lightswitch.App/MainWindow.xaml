<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="Lightswitch.App.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="using:Lightswitch.App"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:tb="using:H.NotifyIcon"
    Title="Lightswitch"
    mc:Ignorable="d">
    <Window.SystemBackdrop>
        <MicaBackdrop />
    </Window.SystemBackdrop>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <TitleBar x:Name="AppTitleBar" Title="Lightswitch">
            <TitleBar.IconSource>
                <ImageIconSource ImageSource="Assets/AppIcon.ico" />
            </TitleBar.IconSource>
        </TitleBar>

        <Frame x:Name="RootFrame" Grid.Row="1" />

        <tb:TaskbarIcon
            x:Name="TrayIcon"
            DataContext="{x:Bind ViewModel}"
            Grid.RowSpan="2"
            IconSource="Assets/AppIcon.ico"
            LeftClickCommand="{x:Bind ShowSwitchPopupCommand}"
            MenuActivation="RightClick"
            NoLeftClickDelay="True"
            ToolTipText="Lightswitch">
            <tb:TaskbarIcon.ContextFlyout>
                <MenuFlyout>
                    <MenuFlyoutItem Click="TogglePowerMenuItem_Click" Text="Skru av/pa" />
                    <MenuFlyoutSubItem Text="Brightness">
                        <MenuFlyoutItem Click="Brightness25MenuItem_Click" Text="25%" />
                        <MenuFlyoutItem Click="Brightness50MenuItem_Click" Text="50%" />
                        <MenuFlyoutItem Click="Brightness75MenuItem_Click" Text="75%" />
                        <MenuFlyoutItem Click="Brightness100MenuItem_Click" Text="100%" />
                    </MenuFlyoutSubItem>
                    <MenuFlyoutSeparator />
                    <ToggleMenuFlyoutItem
                        Click="StartWithWindowsMenuItem_Click"
                        IsChecked="{Binding StartWithWindows, Mode=TwoWay}"
                        Text="Start with Windows" />
                    <MenuFlyoutItem Click="OpenMenuItem_Click" Text="Innstillinger" />
                    <MenuFlyoutSeparator />
                    <MenuFlyoutItem Click="ExitMenuItem_Click" Text="Avslutt" />
                </MenuFlyout>
            </tb:TaskbarIcon.ContextFlyout>
        </tb:TaskbarIcon>
    </Grid>
</Window>
